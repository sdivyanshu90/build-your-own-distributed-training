# Core. PyTorch >= 2.3 for the stable torch.distributed.fsdp + torch.distributed.tensor APIs.
torch>=2.3,<2.7
numpy>=1.24
pyyaml>=6.0

# Tokenizer (optional at runtime; tests can use the dependency-free ByteTokenizer).
transformers>=4.40

# Dev / test / lint.
pytest>=8.0
ruff>=0.5
mypy>=1.10
